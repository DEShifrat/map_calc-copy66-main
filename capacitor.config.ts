import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mapcalccopy66main',
  appName: 'map_calc-copy66-main',
  webDir: 'dist'
};

export default config;
